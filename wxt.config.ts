import { defineConfig } from "wxt";

export default defineConfig({
  manifest: {
    name: "gdocst-editor",
    description:
      "This is a prototype extesnion to help devs understand how they can edit google docs using extensions",
    version: "1.0.0",
  },
});
